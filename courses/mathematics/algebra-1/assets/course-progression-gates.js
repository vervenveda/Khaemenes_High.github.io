(()=>{
"use strict";
const MASTERY=80,parse=(key,f={})=>{try{return JSON.parse(localStorage.getItem(key)||"null")||f}catch{return f}};
function unitMastered(n){if(n===1)return Number(parse("khaemenes-algebra1-unit01-a3-v1",{best:{}})?.best?.mastery)>=MASTERY;const p=parse(`khaemenes-algebra1-unit${String(n).padStart(2,"0")}-progress-v1`,{completed:[],scores:{}});return Number(p?.scores?.mastery)>=MASTERY||Array.isArray(p?.completed)&&p.completed.includes("mastery")}
function boot(){const section=document.getElementById("units"),grid=section?.querySelector(".card-grid");if(!grid)return;const cards=[...grid.querySelectorAll(':scope > a.card[href*="units/unit-"]')];cards.forEach((card,i)=>{const unit=i+1,unlocked=unit===1||unitMastered(unit-1),mastered=unitMastered(unit);if(mastered){const p=document.createElement("p");p.className="eyebrow";p.textContent="✓ 80% unit mastery demonstrated";card.prepend(p)}if(!unlocked){card.dataset.locked="true";card.setAttribute("aria-disabled","true");card.removeAttribute("href");const p=document.createElement("p");p.className="notice";p.textContent=`Locked · Reach 80% mastery on Unit ${String(unit-1).padStart(2,"0")} first.`;card.append(p);card.addEventListener("click",e=>e.preventDefault())}});const note=document.createElement("p");note.className="notice";note.innerHTML="<strong>Strict progression:</strong> each lesson check requires 80% to unlock the next lesson; each unit mastery check requires 80% to unlock the next unit; graded weekly checks and cumulative exams follow the same gate.";section.querySelector("h2")?.after(note)}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();
})();