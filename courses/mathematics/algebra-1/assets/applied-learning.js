(()=>{"use strict";const host=document.getElementById("appliedHost");if(!host)return;fetch("../applied-learning/registry.json").then(r=>r.json()).then(data=>{const mode=document.body.dataset.appliedMode||"labs";const items=mode==="projects"?data.projects:data.labs;host.innerHTML=items.map(item=>{const units=item.units?`Units ${item.units.join(", ")}`:`Unit ${String(item.unit).padStart(2,"0")}`;const skills=(item.skills||[]).map(s=>`<code>${s}</code>`).join(" ");const ev=item.deliverables||item.evidence||[];const rubric=item.rubric?`<details><summary>Rubric</summary><ul>${Object.entries(item.rubric).map(([k,v])=>`<li>${k.replaceAll("_"," ")}: ${v}%</li>`).join("")}</ul></details>`:"";return `<article class="card"><p class="eyebrow">${item.id} · ${units}</p><h2>${item.title}</h2><p>${item.task}</p><p><strong>Skill links:</strong> ${skills}</p><h3>Evidence / Deliverables</h3><ul>${ev.map(x=>`<li>${x}</li>`).join("")}</ul>${rubric}${item.remediation_use?`<p class="notice"><strong>Remediation role:</strong> ${item.remediation_use}</p>`:""}</article>`}).join("");}).catch(()=>{host.innerHTML='<article class="card"><h2>Applied learning registry unavailable</h2><p>Return to the course home and try again. Core course completion does not depend on this page loading.</p></article>'});})();