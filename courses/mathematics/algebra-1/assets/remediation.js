(()=>{"use strict";const host=document.getElementById("remediationHost");if(!host)return;const params=new URLSearchParams(location.search),unitNum=Number(params.get("unit")||0),skill=(params.get("skill")||"").trim(),qid=(params.get("question")||"").trim();Promise.all([fetch("../remediation/resource-registry.json").then(r=>r.json()),fetch("../applied-learning/registry.json").then(r=>r.json())]).then(([reg,applied])=>{const esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;","'":"&#39;"}[c]));const units=reg.units||[];const unit=units.find(u=>u.unit===unitNum)||null;const lab=(applied.labs||[]).find(x=>Number(x.unit)===unitNum&&(skill?x.skills?.includes(skill):true));const projects=(applied.projects||[]).filter(x=>(x.units||[]).includes(unitNum)&&(skill?x.skills?.includes(skill):true));if(!unit){host.innerHTML=`<article class="card"><h2>Choose a unit</h2><p>Select the unit where you want support. If you arrived here after an assessment, use the exact unit and skill shown in your result.</p><div class="lesson-grid">${units.map(u=>`<a class="card lesson-card" href="?unit=${u.unit}"><span class="pill">Unit ${String(u.unit).padStart(2,"0")}</span><h3>${esc(u.focus)}</h3></a>`).join("")}</div></article>`;return}const tags=(unit.game_tags||[]).map(x=>`<code>${esc(x)}</code>`).join(" ");host.innerHTML=`<article class="card"><p class="eyebrow">Unit ${String(unit.unit).padStart(2,"0")}${skill?` · ${esc(skill)}`:""}${qid?` · ${esc(qid)}`:""}</p><h2>${esc(unit.focus)}</h2><p>Choose the support level that matches the evidence. Foundation rebuilds prerequisites, Core targets the exact course skill, and Extended strengthens transfer and application.</p><div class="resource-grid"><a class="card" href="${unit.foundation}"><h3>Foundation</h3><p>Rebuild prerequisite understanding and revisit the unit instruction.</p></a><a class="card" href="${unit.core}"><h3>Core Practice</h3><p>Direct corrective practice for this Algebra I unit.</p></a><a class="card" href="${unit.extended}"><h3>Extended</h3><p>Apply and strengthen the skill after core understanding is stable.</p></a>${lab?`<a class="card" href="../labs/#${lab.id}"><h3>${esc(lab.title)}</h3><p>Applied lab connected to this unit${skill?" and skill":""}.</p></a>`:""}${projects.slice(0,2).map(p=>`<a class="card" href="../projects/#${p.id}"><h3>${esc(p.title)}</h3><p>Cumulative application using related skills.</p></a>`).join("")}</div><p class="notice"><strong>NAIB-ready game tags:</strong> ${tags||"unit practice"}. NAIB may recommend a matching game, but local course recovery remains complete without it.</p><div class="actions"><a class="btn primary" href="../units/unit-${String(unit.unit).padStart(2,"0")}/">Return to Unit ${String(unit.unit).padStart(2,"0")}</a><a class="btn" href="../assessments/">Reassessment Center</a><a class="btn" href="../">Course Home</a></div></article>`}).catch(()=>{host.innerHTML='<article class="card"><h2>Recovery registry unavailable</h2><p>Return to the unit and use its Foundation, Core, or Extended practice. Course completion does not depend on this router.</p><a class="btn" href="../">Course Home</a></article>'});})();