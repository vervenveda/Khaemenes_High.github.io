(()=>{"use strict";
if(!document.getElementById("weekList")||typeof APP==="undefined"||typeof dashboard!=="function")return;

const MIN=80;
const COURSE_KEY="KHAE_OPEN_PREALGEBRA_FORGE_V2";
const LEGACY_COURSE_KEYS=["KHAE_MATH9_PREALGEBRA_FORGE_V1"];
const MIDTERM_RESULT_KEY="khaemenes-prealgebra-midterm-result-v1";
const FINAL_RESULT_KEY="khaemenes-prealgebra-final-result-v1";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const pad=n=>String(Number(n)||0).padStart(2,"0");
const safeNumber=v=>Number.isFinite(Number(v))?Number(v):0;
function readJSON(key,fallback=null){try{const raw=localStorage.getItem(key);if(!raw||raw.length>2_000_000)return fallback;const value=JSON.parse(raw);return value&&typeof value==="object"?value:fallback}catch{return fallback}}
function activeLearner(){for(const key of [COURSE_KEY,...LEGACY_COURSE_KEYS]){const state=readJSON(key);if(!state||!Array.isArray(state.students))continue;const learner=state.students.find(s=>s&&s.id===state.activeId)||state.students[0]||null;if(learner)return learner}return null}
function bestResult(record){if(!record||typeof record!=="object")return 0;const attempts=Array.isArray(record.attempts)?record.attempts:[];return Math.max(safeNumber(record.best_percent),safeNumber(record.percent),safeNumber(record.latest_percent),safeNumber(record.best),...attempts.map(a=>safeNumber(a?.percent||a?.score)))}
function latestResult(record){if(!record||typeof record!=="object")return 0;const attempts=Array.isArray(record.attempts)?record.attempts:[];const last=attempts.at(-1);return safeNumber(last?.percent||last?.score||record.latest_percent||record.percent||record.best_percent||record.best)}
function scoreState(score){return score>=MIN?"score-good":score>0?"score-review":"score-empty"}
function scoreLabel(score){return score>0?`${Math.round(score)}%`:"—"}
function weeklyEvidence(learner){const weeks=learner?.progress?.weeks&&typeof learner.progress.weeks==="object"?learner.progress.weeks:{};return Array.from({length:36},(_,i)=>{const week=i+1,record=weeks[week]||weeks[String(week)]||{},quiz=record.quiz&&typeof record.quiz==="object"?record.quiz:{},attempts=Array.isArray(quiz.attempts)?quiz.attempts:[],best=Math.max(safeNumber(quiz.best),...attempts.map(a=>safeNumber(a?.score))),latest=attempts.length?safeNumber(attempts.at(-1)?.score):best,feedback=Array.isArray(quiz.lastFeedback)?quiz.lastFeedback.filter(Boolean):[],title=APP.weeks?.find(w=>Number(w.week)===week)?.title||`Week ${week}`;return{week,title,best,latest,attempts,feedback}})}
function unitEvidence(){return Array.from({length:13},(_,i)=>{const unit=i+1,record=readJSON(`khaemenes-prealgebra-u${pad(unit)}-mastery-v1`),best=bestResult(record),latest=latestResult(record),attempts=Array.isArray(record?.attempts)?record.attempts.length:(best?1:0);return{unit,best,latest,attempts,record}})}
function examEvidence(key,label,href){const r=readJSON(key),selected=safeNumber(r?.selected_response_percent||r?.percent),constructed=safeNumber(r?.constructed_response?.percent),overall=safeNumber(r?.overall_percent),reviewComplete=!!r?.constructed_response?.review_complete,mastered=!!r?.mastery_met||overall>=MIN,status=!r?"No official result yet":mastered?"Mastery recorded":reviewComplete?"Evaluator review complete":"Pending / review required";return{label,href,r,selected,constructed,overall,reviewComplete,mastered,status}}
async function unitTitles(){try{const response=await fetch("course-map.json",{cache:"no-cache"});if(!response.ok)throw new Error(String(response.status));const map=await response.json(),titles={};(map.units||[]).forEach(u=>{const n=Number(u.number);if(n>=1&&n<=13)titles[n]=u.title||`Unit ${n}`});return titles}catch{return Object.fromEntries(Array.from({length:13},(_,i)=>[i+1,`Unit ${i+1}`]))}}
function feedbackMarkup(row){if(!row.feedback.length)return"";const meaningful=row.feedback.filter(x=>!/all answers were correct/i.test(String(x)));if(!meaningful.length)return'<span class="scorebook-perfect">Last recorded feedback: all answers correct.</span>';return`<details class="scorebook-feedback"><summary>Review ${meaningful.length} missed item${meaningful.length===1?"":"s"} from the latest check</summary><ul>${meaningful.map(item=>`<li>${esc(item)}</li>`).join("")}</ul></details>`}
function weeklyMarkup(rows){const scored=rows.filter(r=>r.best>0),needs=scored.filter(r=>r.best<MIN);return`<section class="scorebook-section"><div class="scorebook-section-head"><div><span class="scorebook-kicker">36 weekly mastery checks</span><h4>Weekly Score History</h4><p>Best score is preserved. Latest shows the most recent submitted attempt. Missed-question feedback is study guidance from that latest check.</p></div><span class="scorebook-summary">${scored.length}/36 scored · ${needs.length} review flag${needs.length===1?"":"s"}</span></div><div class="scorebook-weeks">${rows.map(row=>`<details class="scorebook-week ${scoreState(row.best)}"><summary><span class="scorebook-week-no">W${pad(row.week)}</span><span class="scorebook-week-copy"><strong>${esc(row.title)}</strong><small>${row.attempts.length?`${row.attempts.length} attempt${row.attempts.length===1?"":"s"}`:"No scored attempt"}</small></span><span class="scorebook-score"><strong>${scoreLabel(row.best)}</strong><small>best${row.latest?` · latest ${Math.round(row.latest)}%`:""}</small></span></summary><div class="scorebook-week-detail">${feedbackMarkup(row)}${row.best>0?`<p>${row.best>=MIN?"Mastery standard met.":`Below ${MIN}%: revisit the aligned unit lessons and corrections before the next cumulative assessment.`}</p>`:"<p>No weekly mastery score has been recorded yet.</p>"}</div></details>`).join("")}</div></section>`}
function unitMarkup(rows,titles){const scored=rows.filter(r=>r.best>0),mastered=scored.filter(r=>r.best>=MIN);return`<section class="scorebook-section"><div class="scorebook-section-head"><div><span class="scorebook-kicker">13 unit mastery records</span><h4>Unit Mastery Scores</h4><p>These scores come from the canonical unit mastery records used by the course gates.</p></div><span class="scorebook-summary">${mastered.length}/13 at ${MIN}%+</span></div><div class="scorebook-units">${rows.map(row=>`<article class="scorebook-unit ${scoreState(row.best)}"><span>Unit ${pad(row.unit)}</span><strong>${esc(titles[row.unit]||`Unit ${row.unit}`)}</strong><div><b>${scoreLabel(row.best)}</b><small>${row.best?`best · latest ${Math.round(row.latest||row.best)}% · ${row.attempts||1} attempt${(row.attempts||1)===1?"":"s"}`:"not yet scored"}</small></div><a href="units/unit-${pad(row.unit)}/">${row.best?"Review unit":"Unit doorway"}</a></article>`).join("")}</div></section>`}
function examMarkup(exam){return`<article class="scorebook-exam ${exam.mastered?"score-good":exam.r?"score-review":"score-empty"}"><span class="scorebook-kicker">Official cumulative assessment</span><h4>${esc(exam.label)}</h4><strong class="scorebook-exam-status">${esc(exam.status)}</strong>${exam.r?`<dl><div><dt>Selected response</dt><dd>${scoreLabel(exam.selected)}</dd></div><div><dt>Constructed response</dt><dd>${exam.constructed?scoreLabel(exam.constructed):exam.reviewComplete?"Reviewed":"Pending"}</dd></div><div><dt>Overall</dt><dd>${exam.overall?scoreLabel(exam.overall):"Pending"}</dd></div></dl>`:'<p>No official cumulative result is stored in this browser yet.</p>'}<a class="scorebook-exam-link" href="${exam.href}">${exam.r?"Open assessment record":"Open official assessment"}</a></article>`}
function focusSummary(weeks,units){const weakWeeks=weeks.filter(r=>r.best>0&&r.best<MIN),weakUnits=units.filter(r=>r.best>0&&r.best<MIN);if(!weakWeeks.length&&!weakUnits.length)return`<div class="scorebook-focus score-good"><strong>Study focus</strong><span>No scored weekly or unit record is currently below ${MIN}%. Use cumulative reviews for retrieval practice.</span></div>`;const parts=[];if(weakWeeks.length)parts.push(`Weeks ${weakWeeks.map(r=>r.week).join(", ")}`);if(weakUnits.length)parts.push(`Units ${weakUnits.map(r=>r.unit).join(", ")}`);return`<div class="scorebook-focus score-review"><strong>Study focus</strong><span>Prioritize ${esc(parts.join(" · "))}. These are scored records below the ${MIN}% mastery standard.</span></div>`}
async function buildScorebook(){const learner=activeLearner(),weeks=weeklyEvidence(learner),units=unitEvidence(),titles=await unitTitles(),mid=examEvidence(MIDTERM_RESULT_KEY,"Midterm · Units 01–07","assessments/midterm-units-01-07.html"),fin=examEvidence(FINAL_RESULT_KEY,"Final · 36-Week Course","assessments/final-exam-36-weeks.html");return`<section class="card col12 study-scorebook-card" id="studyScorebookCard"><div class="scorebook-heading"><div><span class="scorebook-kicker">Read-only study evidence</span><h3>Study Scorebook</h3><p>${learner?`Reviewing records for <strong>${esc(learner.name||"current learner")}</strong>.`:"No dashboard learner record is selected yet."} This view reads existing evidence only; it does not submit, reset, lower, or replace any score.</p></div><span class="scorebook-standard">${MIN}% mastery standard</span></div>${focusSummary(weeks,units)}${weeklyMarkup(weeks)}${unitMarkup(units,titles)}<section class="scorebook-section"><div class="scorebook-section-head"><div><span class="scorebook-kicker">Midterm & final</span><h4>Official Cumulative Results</h4><p>These are the official assessment result records, not the optional dashboard practice samplers.</p></div></div><div class="scorebook-exams">${examMarkup(mid)}${examMarkup(fin)}</div></section><p class="scorebook-footnote">Browser-local results are study evidence and remain separate from any signed or administrator-retained school record.</p></section>`}
async function renderStudyScorebook(){const grid=document.querySelector("#content .grid");if(!grid)return;grid.querySelector("#studyScorebookCard")?.remove();const loading=document.createElement("section");loading.id="studyScorebookCard";loading.className="card col12 study-scorebook-card";loading.innerHTML="<p>Loading Study Scorebook…</p>";const navigator=grid.querySelector("#courseNavigatorCard");navigator?navigator.insertAdjacentElement("afterend",loading):grid.appendChild(loading);try{const html=await buildScorebook(),host=document.createElement("div");host.innerHTML=html;document.getElementById("studyScorebookCard")?.replaceWith(host.firstElementChild)}catch(error){console.error("Pre-Algebra Study Scorebook could not load.",error);const card=document.getElementById("studyScorebookCard");if(card)card.innerHTML="<h3>Study Scorebook</h3><p>The score history could not be displayed. Existing records were not changed.</p>"}}
function installStyles(){if(document.getElementById("prealgebraStudyScorebookStyles"))return;const style=document.createElement("style");style.id="prealgebraStudyScorebookStyles";style.textContent=`.study-scorebook-card{grid-column:span 12}.scorebook-heading,.scorebook-section-head{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.scorebook-heading h3,.scorebook-section h4{margin:.2rem 0 .5rem}.scorebook-heading p,.scorebook-section-head p{margin:0;max-width:76ch}.scorebook-kicker{display:block;color:var(--k-burgundy,#71363e);font-size:.68rem;font-weight:700;letter-spacing:.13em;text-transform:uppercase}.scorebook-standard,.scorebook-summary{flex:0 0 auto;padding:.35rem .6rem;border:1px solid var(--k-line,rgba(20,40,32,.14));border-radius:999px;background:#fff;color:var(--k-muted,#66736d);font-size:.73rem}.scorebook-focus{display:grid;grid-template-columns:auto 1fr;gap:10px;margin:18px 0;padding:12px 14px;border-radius:7px;border:1px solid var(--k-line,rgba(20,40,32,.14))}.scorebook-focus.score-review{border-left:3px solid #b48b45;background:#fff7e5}.scorebook-focus.score-good{border-left:3px solid #5f8a69;background:#eef8f0}.scorebook-section{margin-top:24px;padding-top:20px;border-top:1px solid var(--k-line,rgba(20,40,32,.14))}.scorebook-weeks{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:14px}.scorebook-week{border:1px solid var(--k-line,rgba(20,40,32,.14));border-radius:8px;background:#fff;overflow:hidden}.scorebook-week summary{display:grid;grid-template-columns:48px minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px 12px;cursor:pointer;list-style:none}.scorebook-week summary::-webkit-details-marker{display:none}.scorebook-week.score-good{border-left:3px solid #5f8a69}.scorebook-week.score-review{border-left:3px solid #b48b45}.scorebook-week-no{font-size:.72rem;font-weight:800;color:var(--k-burgundy,#71363e)}.scorebook-week-copy,.scorebook-score{display:grid;gap:1px}.scorebook-week-copy small,.scorebook-score small{color:var(--k-muted,#66736d);font-size:.7rem}.scorebook-score{text-align:right}.scorebook-score strong{color:var(--k-forest,#17392f)}.scorebook-week-detail{padding:0 12px 12px;color:var(--k-muted,#66736d);font-size:.8rem}.scorebook-feedback{padding:10px;border-radius:7px;background:#f8f3e8}.scorebook-feedback summary{display:block;padding:0;font-weight:700;color:var(--k-forest,#17392f)}.scorebook-feedback ul{margin:.6rem 0 0;padding-left:1.2rem}.scorebook-perfect{color:#326444}.scorebook-units{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin-top:14px}.scorebook-unit{display:grid;gap:5px;padding:12px;border:1px solid var(--k-line,rgba(20,40,32,.14));border-radius:8px;background:#fff}.scorebook-unit.score-good{border-top:3px solid #5f8a69}.scorebook-unit.score-review{border-top:3px solid #b48b45}.scorebook-unit>span{color:var(--k-burgundy,#71363e);font-size:.68rem;font-weight:800;text-transform:uppercase}.scorebook-unit>strong{font-family:var(--k-formal,Georgia,serif);font-weight:500}.scorebook-unit>div{display:grid;gap:1px}.scorebook-unit b{font-size:1.15rem;color:var(--k-forest,#17392f)}.scorebook-unit small{color:var(--k-muted,#66736d)}.scorebook-unit a{justify-self:start;font-size:.76rem}.scorebook-exams{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:14px}.scorebook-exam{padding:16px;border:1px solid var(--k-line,rgba(20,40,32,.14));border-radius:9px;background:#fbf9f4}.scorebook-exam.score-good{border-top:3px solid #5f8a69}.scorebook-exam.score-review{border-top:3px solid #b48b45}.scorebook-exam-status{display:block;margin:.4rem 0 .7rem;color:var(--k-forest,#17392f)}.scorebook-exam dl{display:grid;gap:5px;margin:10px 0}.scorebook-exam dl div{display:flex;justify-content:space-between;gap:12px;padding:6px 0;border-bottom:1px solid var(--k-line,rgba(20,40,32,.10))}.scorebook-exam dt{color:var(--k-muted,#66736d)}.scorebook-exam dd{margin:0;font-weight:700}.scorebook-exam-link{display:inline-flex;margin-top:8px;font-size:.78rem}.scorebook-footnote{margin:18px 0 0;color:var(--k-muted,#66736d);font-size:.76rem}@media(max-width:900px){.scorebook-weeks,.scorebook-exams{grid-template-columns:1fr}.scorebook-units{grid-template-columns:repeat(2,minmax(0,1fr))}.scorebook-heading,.scorebook-section-head{flex-direction:column}.scorebook-week summary{grid-template-columns:42px minmax(0,1fr)}.scorebook-score{grid-column:2;text-align:left}}@media(max-width:560px){.scorebook-units{grid-template-columns:1fr}.scorebook-focus{grid-template-columns:1fr}}@media print{.scorebook-week{break-inside:avoid}.scorebook-feedback{display:none}.scorebook-exam-link,.scorebook-unit a{display:none!important}}`;document.head.appendChild(style)}

installStyles();
const priorDashboard=dashboard;
dashboard=function(){priorDashboard();renderStudyScorebook()};
if(document.querySelector("#content .grid"))renderStudyScorebook();
window.addEventListener("storage",event=>{const k=event.key||"";if(k===COURSE_KEY||LEGACY_COURSE_KEYS.includes(k)||k.startsWith("khaemenes-prealgebra-u")||k===MIDTERM_RESULT_KEY||k===FINAL_RESULT_KEY)renderStudyScorebook()});
})();
