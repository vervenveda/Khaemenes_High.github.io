import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');let failures=[];const read=p=>fs.readFileSync(path.join(root,p),'utf8');
function must(cond,msg){if(!cond)failures.push(msg)}
const course=JSON.parse(read('course-map.json'));must(course.course?.duration_weeks===36,'course-map: official duration must be 36 weeks');must(course.course?.supported_duration_weeks===42,'course-map: supported duration must be 42 weeks');must(course.readiness_gateway?.counts_toward_official_duration===false,'course-map: readiness assessment must not count as a course week');must(course.readiness_gateway?.mastery_threshold_percent===80,'course-map: readiness threshold must be 80%');must(course.conditional_unit_0?.duration_weeks===6,'course-map: conditional Unit 0 must be six weeks');must(course.conditional_unit_0?.supported_pathway_total_weeks===42,'course-map: Unit 0 supported pathway must total 42 weeks');
const u0=JSON.parse(read('units/unit-00/unit-map.json'));must(u0.unit?.duration_weeks===6,'Unit 0 map: duration must be six weeks');must(u0.unit?.official_course_duration_weeks===36,'Unit 0 map: official course must remain 36 weeks');must(u0.unit?.supported_course_duration_weeks===42,'Unit 0 map: supported route must be 42 weeks');must(u0.unit?.lesson_mastery_threshold_percent===80,'Unit 0 map: lesson threshold must be 80%');must(u0.unit?.weekly_quiz_threshold_percent===80,'Unit 0 map: weekly quiz threshold must be 80%');must(u0.unit?.exit_threshold_percent===80,'Unit 0 map: exit threshold must be 80%');must(Array.isArray(u0.lessons)&&u0.lessons.length===6,'Unit 0 map: exactly six refresher weeks required');
for(let n=2;n<=13;n++){const le=read(`units/unit-${String(n).padStart(2,'0')}/assets/lesson-engine.js`);must(!/percent\s*>=\s*70|>=\s*70/.test(le),`Unit ${n}: legacy 70% threshold found`);must(/MIN\s*=\s*Math\.max\(80/.test(le),`Unit ${n}: hardened 80% lesson engine missing`);must(/lessonBestScores/.test(le)&&/lessonAttempts/.test(le),`Unit ${n}: best-score attempt history missing`);must(/aria-disabled/.test(le)&&/renderNavigation/.test(le),`Unit ${n}: sequential next-lesson gate missing`);const dash=read(`units/unit-${String(n).padStart(2,'0')}/assets/unit-dashboard.js`);must(/Prior lesson requires 80%/.test(dash),`Unit ${n}: dashboard sequential lesson gate missing`);must(/data-review/.test(dash)&&!/data-complete/.test(dash),`Unit ${n}: dashboard review/mastery separation missing`)}
for(let n=1;n<=13;n++){const ae=read(`units/unit-${String(n).padStart(2,'0')}/assets/assessment-engine.js`);must(/best_percent/.test(ae)&&/attempts/.test(ae),`Unit ${n}: mastery assessment attempt history/best mastery missing`);must(/MIN=80/.test(ae),`Unit ${n}: mastery assessment wrapper must enforce 80%`)}
const rootGate=read('assets/prealgebra-course-gates-v1.js');must(/NAIB_KEY/.test(rootGate)&&/EXIT_KEY/.test(rootGate),'Root gate: NAIB/Unit0 entrance routing missing');must(/weekMastered/.test(rootGate),'Root gate: prior weekly quiz 80% gate missing');
const u0Index=read('units/unit-00/index.html');must(/36/.test(u0Index)&&/42/.test(u0Index)&&/practice \+ quiz both require 80%/.test(u0Index),'Unit 0 index: 36/42 and weekly gating language missing');for(let n=1;n<=6;n++){const names=['number-foundations','rational-proportional','algebraic-reasoning','functions-coordinates','geometry-measurement','data-probability'];const f=read(`units/unit-00/lessons/lesson-${String(n).padStart(2,'0')}-${names[n-1]}.html`);must(/20 Questions/.test(f),`Unit0 Week ${n}: 20-question lesson practice missing`);must(/10 Questions/.test(f),`Unit0 Week ${n}: 10-question weekly quiz missing`);must(/Reach 80%/.test(f),`Unit0 Week ${n}: 80% gate language missing`)}
const exit=read('units/unit-00/assessment/exit-mastery.html');must(/Parallel Form A/.test(exit)&&/"B"/.test(exit),'Unit0 exit: parallel forms missing');must(/requires 80%/.test(exit),'Unit0 exit: 80% exit gate missing');
if(failures.length){console.error('\nPRE-ALGEBRA MASTERY GATE VALIDATION FAILED');for(const f of failures)console.error(' - '+f);process.exit(1)}console.log('OK Pre-Algebra Academy Mastery Contract');console.log(' - readiness assessment is pre-course and non-week');console.log(' - official route: 36 weeks');console.log(' - NAIB refresher route: 6 + 36 = 42 weeks');console.log(' - lesson, weekly quiz, unit, and Unit 0 exit gates: 80%');console.log(' - reviewed is separate from mastered');console.log(' - best demonstrated mastery and attempt history preserved');
