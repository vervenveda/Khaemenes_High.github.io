"use strict";
(() => {
  const KEY="khaemenes_science_u02_v1",ROOT_KEY="khaemenes_science_integrated9_v1",THEME_KEY="khaemenes_theme";
  const REQUIREMENTS=["day01","day02","day03","day04","day05","day06","day07","day08","day09","day10","day11","day12","day13","day14","day15","microscopy","diffusion","transport","sav","model","quiz","assessment","verification","reflection"];
  const DEFAULT={
    completedPages:[],
    microscopy:{passed:false,score:0,total:6,bestScore:0,bestPercent:0,attempts:0},
    diffusion:{complete:false},transport:{complete:false},sav:{complete:false},
    model:{submitted:false,title:"",completedAt:null},
    quiz:{passed:false,score:0,total:20,bestScore:0,bestPercent:0,attempts:0},
    assessment:{passed:false,score:0,total:30,bestScore:0,bestPercent:0,attempts:0},
    verification:{complete:false,modelScore:null,modelTotal:40,cr1Score:null,cr1Total:20,cr2Score:null,cr2Total:20,reviewer:"",reviewedAt:null},
    reflection:{complete:false,text:""},updatedAt:null
  };
  const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];
  function parse(raw,fallback){try{return JSON.parse(raw)??fallback}catch{return fallback}}
  function rootState(){return parse(localStorage.getItem(ROOT_KEY),{})}
  function prerequisiteMet(){const r=rootState();return Array.isArray(r.completedUnits)&&r.completedUnits.includes("u01")}
  function load(){const s=parse(localStorage.getItem(KEY),{});return {...DEFAULT,...s,completedPages:Array.isArray(s.completedPages)?s.completedPages:[],microscopy:{...DEFAULT.microscopy,...(s.microscopy||{})},diffusion:{...DEFAULT.diffusion,...(s.diffusion||{})},transport:{...DEFAULT.transport,...(s.transport||{})},sav:{...DEFAULT.sav,...(s.sav||{})},model:{...DEFAULT.model,...(s.model||{})},quiz:{...DEFAULT.quiz,...(s.quiz||{})},assessment:{...DEFAULT.assessment,...(s.assessment||{})},verification:{...DEFAULT.verification,...(s.verification||{})},reflection:{...DEFAULT.reflection,...(s.reflection||{})}}}
  let state=load();
  function quizPassed(){return !!state.quiz.passed&&Number(state.quiz.bestScore||state.quiz.score||0)>=16}
  function assessmentPassed(){return !!state.assessment.passed&&Number(state.assessment.bestScore||state.assessment.score||0)>=24}
  function microscopyPassed(){return !!state.microscopy.passed&&Number(state.microscopy.bestScore||state.microscopy.score||0)>=5}
  function verificationPassed(){const v=state.verification||{};return !!v.complete&&Number(v.modelScore)>=32&&Number(v.cr1Score)>=16&&Number(v.cr2Score)>=16}
  function done(id){if(/^day\d\d$/.test(id))return state.completedPages.includes(id);if(id==="microscopy")return microscopyPassed();if(["diffusion","transport","sav"].includes(id))return !!state[id].complete;if(id==="model")return !!state.model.submitted&&Number(state.verification.modelScore)>=32;if(id==="quiz")return quizPassed();if(id==="assessment")return assessmentPassed();if(id==="verification")return verificationPassed();if(id==="reflection")return !!state.reflection.complete;return false}
  function complete(){return prerequisiteMet()&&REQUIREMENTS.every(done)}
  function syncRoot(){const r=rootState(),set=new Set(Array.isArray(r.completedUnits)?r.completedUnits:[]),isComplete=complete();if(isComplete)set.add("u02");else set.delete("u02");const courseSequenceComplete=["u00","u01","u02","u03","u04","u05","u06","u07","u08","u09","u10","u11","u12"].every(id=>set.has(id));localStorage.setItem(ROOT_KEY,JSON.stringify({...r,pathway:r.pathway||"Core",completedUnits:[...set],notes:r.notes&&typeof r.notes==="object"?r.notes:{},lastVisitedUnit:prerequisiteMet()?"u02":(r.lastVisitedUnit||"u01"),courseSequenceComplete,updatedAt:new Date().toISOString()}))}
  function save(){if(!prerequisiteMet()){showPrerequisiteGate();return}state.updatedAt=new Date().toISOString();localStorage.setItem(KEY,JSON.stringify(state));syncRoot();update()}
  function theme(t){const v=["dark","light"].includes(t)?t:"dark";document.documentElement.dataset.theme=v;localStorage.setItem(THEME_KEY,v);const b=$("#themeToggle");if(b){b.textContent=v==="dark"?"☼":"◐";b.setAttribute("aria-label",`Switch to ${v==="dark"?"light":"dark"} theme`)}}
  function toast(m){const b=$("#toast");if(!b)return;b.textContent=m;b.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>b.hidden=true,3200)}
  function repairPortalLinks(){const repositoryRoot=location.pathname.includes("/lessons/")?"../../../../../../":"../../../../../";$$('a').forEach(link=>{const label=link.textContent.replace(/\s+/g," ").trim();if(label==="High School")link.href=`${repositoryRoot}index.html`;else if(label==="Grade 9")link.href=`${repositoryRoot}grades/grade-09/`})}
  function showPrerequisiteGate(){let gate=$("#sciencePrerequisiteGate");if(!gate){gate=document.createElement("section");gate.id="sciencePrerequisiteGate";gate.setAttribute("role","status");gate.style.cssText="position:relative;z-index:50;width:min(920px,calc(100% - 32px));margin:28px auto;padding:24px;border:2px solid #a67b35;border-radius:14px;background:#fff8e7;color:#1d2730;box-shadow:0 14px 34px rgba(0,0,0,.14);text-align:center";const back=location.pathname.includes("/lessons/")?"../../unit-01/":"../unit-01/";gate.innerHTML=`<p style="margin:0 0 8px;font-size:.8rem;letter-spacing:.12em;text-transform:uppercase;color:#7a5b1f">Mastery prerequisite</p><h2 style="margin:.2rem 0 .7rem">Unit 02 is locked</h2><p style="max-width:720px;margin:0 auto 16px;line-height:1.6">Unit 01 — Scientific Inquiry, Measurement &amp; Evidence must be fully completed and mastery-verified before Unit 02 progress can be recorded.</p><a href="${back}" style="display:inline-flex;padding:10px 16px;border-radius:7px;background:#17344a;color:#fff;text-decoration:none">Return to Unit 01</a>`;($("main")||document.body).prepend(gate)}$$('[data-page-complete],[data-evidence],#modelForm button[type="submit"],#reflectionForm button[type="submit"],#verificationForm button[type="submit"]').forEach(el=>{el.disabled=true;el.setAttribute("aria-disabled","true")});$$('a[href*="mastery"],a[href*="assessment"],a[href*="lessons/"]').forEach(a=>{a.setAttribute("aria-disabled","true");a.onclick=e=>{if(!prerequisiteMet()){e.preventDefault();gate.scrollIntoView({behavior:"smooth",block:"center"})}})} )}
  function toggle(id){if(!prerequisiteMet())return showPrerequisiteGate();const s=new Set(state.completedPages);s.has(id)?s.delete(id):s.add(id);state.completedPages=[...s];save();toast(s.has(id)?"Lesson marked complete.":"Lesson completion removed.")}
  function evidence(type){if(!prerequisiteMet())return showPrerequisiteGate();state[type]={...state[type],complete:true,completedAt:new Date().toISOString()};save();toast("Evidence recorded.")}
  function submitVerification(e){e.preventDefault();if(!prerequisiteMet())return showPrerequisiteGate();const modelScore=Number($("#modelScore")?.value),cr1Score=Number($("#cr1Score")?.value),cr2Score=Number($("#cr2Score")?.value),reviewer=$("#reviewerName")?.value.trim()||"",ack=!!$("#verificationAck")?.checked;if(!Number.isFinite(modelScore)||modelScore<0||modelScore>40||!Number.isFinite(cr1Score)||cr1Score<0||cr1Score>20||!Number.isFinite(cr2Score)||cr2Score<0||cr2Score>20){toast("Enter valid scores: model 0–40; each constructed response 0–20.");return}if(!reviewer||!ack){toast("Enter the reviewer name and confirm that the evidence was reviewed.");return}const ok=modelScore>=32&&cr1Score>=16&&cr2Score>=16;state.verification={complete:ok,modelScore,modelTotal:40,cr1Score,cr1Total:20,cr2Score,cr2Total:20,reviewer,reviewedAt:new Date().toISOString()};save();toast(ok?"Human-scored Unit 02 mastery verified.":"Scores recorded. One or more performance components remain below 80%.")}
  function scoreText(record,threshold){const best=Number(record.bestScore||record.score||0),total=Number(record.total||0);return `${best} of ${total}${best>=threshold?" · Mastery":""}`}
  function update(){if(!prerequisiteMet()){showPrerequisiteGate();if($("#unitCompletionStatus"))$("#unitCompletionStatus").textContent="Unit 02 locked · Unit 01 mastery required";return}$("#sciencePrerequisiteGate")?.remove();$$("[data-requirement]").forEach(c=>{const v=done(c.dataset.requirement);c.dataset.complete=String(v);const s=$("[data-requirement-status]",c);if(s)s.textContent=v?"Complete":"Not complete"});const count=REQUIREMENTS.filter(done).length,pct=Math.round(count/REQUIREMENTS.length*100);if($("#unitProgressFill"))$("#unitProgressFill").style.width=`${pct}%`;if($("#unitProgressCount"))$("#unitProgressCount").textContent=`${count} of ${REQUIREMENTS.length} requirements`;if($("#unitProgressPercent"))$("#unitProgressPercent").textContent=`${pct}%`;if($("#unitCompletionStatus"))$("#unitCompletionStatus").textContent=complete()?"Unit 02 complete · mastery verified":"Unit 02 in progress";$$("[data-page-complete]").forEach(b=>{const v=state.completedPages.includes(b.dataset.pageComplete);b.disabled=false;b.removeAttribute("aria-disabled");b.setAttribute("aria-pressed",String(v));b.textContent=v?"Lesson Complete ✓":"Mark Lesson Complete"});$$("[data-evidence]").forEach(b=>{const v=!!state[b.dataset.evidence]?.complete;b.disabled=v;b.textContent=v?"Evidence Recorded ✓":"Record Evidence Complete"});if($("#modelTitle"))$("#modelTitle").value=state.model.title||"";if($("#reflectionText"))$("#reflectionText").value=state.reflection.text||"";if($("#modelScore")&&state.verification.modelScore!=null)$("#modelScore").value=state.verification.modelScore;if($("#cr1Score")&&state.verification.cr1Score!=null)$("#cr1Score").value=state.verification.cr1Score;if($("#cr2Score")&&state.verification.cr2Score!=null)$("#cr2Score").value=state.verification.cr2Score;if($("#reviewerName"))$("#reviewerName").value=state.verification.reviewer||"";if($("#recordStatus")){ $("#recordDate").textContent=new Date().toLocaleDateString();$("#recordStatus").textContent=complete()?"Complete · mastery verified":"In progress";$("#recordLessons").textContent=`${state.completedPages.filter(x=>/^day\d\d$/.test(x)).length} of 15`;$("#recordLabs").textContent=`${Number(microscopyPassed())+Number(state.diffusion.complete)+Number(state.transport.complete)+Number(state.sav.complete)} of 4`;$("#recordModel").textContent=state.model.submitted?(Number(state.verification.modelScore)>=32?`${state.verification.modelScore}/40 · Mastery verified":"Submitted · awaiting/needs rubric mastery"):"Not submitted";$("#recordQuiz").textContent=scoreText(state.quiz,16);$("#recordAssessment").textContent=scoreText(state.assessment,24);if($("#recordConstructed"))$("#recordConstructed").textContent=state.verification.reviewedAt?`CR1 ${state.verification.cr1Score}/20 · CR2 ${state.verification.cr2Score}/20${verificationPassed()?" · Mastery verified":""}`:"Awaiting human scoring";if($("#recordVerification"))$("#recordVerification").textContent=verificationPassed()?"Verified":"Not yet verified";$("#recordReflection").textContent=state.reflection.complete?"Recorded":"Not recorded";$("#recordReflectionText").textContent=state.reflection.text||"No reflection recorded."}}
  function exportRecord(){if(!prerequisiteMet())return showPrerequisiteGate();const payload={schema:"khaemenes-science-u02-v2",course:"KH-SCI-IIS9",unit:"u02",prerequisiteUnit01Verified:true,masteryVerified:complete(),exportedAt:new Date().toISOString(),state};const blob=new Blob([JSON.stringify(payload,null,2)],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`KH-SCI-IIS9-unit02-record-${new Date().toISOString().slice(0,10)}.json`;document.body.append(a);a.click();a.remove();URL.revokeObjectURL(url);toast("Unit 02 record exported.")}
  function init(){repairPortalLinks();theme(localStorage.getItem(THEME_KEY)||(matchMedia("(prefers-color-scheme: light)").matches?"light":"dark"));$("#themeToggle")?.addEventListener("click",()=>theme(document.documentElement.dataset.theme==="dark"?"light":"dark"));$$("[data-page-complete]").forEach(b=>b.addEventListener("click",()=>toggle(b.dataset.pageComplete)));$$("[data-evidence]").forEach(b=>b.addEventListener("click",()=>evidence(b.dataset.evidence)));$("#modelForm")?.addEventListener("submit",e=>{e.preventDefault();if(!prerequisiteMet())return showPrerequisiteGate();const checks=$$(".model-check");if(!checks.length||!checks.every(x=>x.checked)){toast("Complete every model criterion.");return}state.model={submitted:true,title:$("#modelTitle")?.value.trim()||"Cell model",completedAt:new Date().toISOString()};save();toast("Cell model submitted; rubric mastery verification is still required.")});$("#verificationForm")?.addEventListener("submit",submitVerification);$("#reflectionForm")?.addEventListener("submit",e=>{e.preventDefault();if(!prerequisiteMet())return showPrerequisiteGate();const text=$("#reflectionText")?.value.trim()||"";if(text.length<80){toast("Add a substantive reflection of at least 80 characters.");return}state.reflection={complete:true,text,completedAt:new Date().toISOString()};save();toast("Reflection recorded.")});$("#printPage")?.addEventListener("click",()=>window.print());$("#exportUnitRecord")?.addEventListener("click",exportRecord);syncRoot();update()}
  window.KhaemenesUnit02={loadState:()=>state,prerequisiteMet,masteryVerified:complete,saveState(next){if(!prerequisiteMet())return showPrerequisiteGate();state={...state,...next};save()},toast};
  document.addEventListener("DOMContentLoaded",init);
})();
(() => {const marker="/courses/science/integrated-science-9/",path=window.location.pathname,at=path.indexOf(marker),base=at>=0?path.slice(0,at+marker.length):new URL("./",window.location.href).pathname;if(document.querySelector('script[data-science-theme-loader]'))return;const script=document.createElement("script");script.src=`${base}science-theme-loader.js`;script.defer=true;script.dataset.scienceThemeLoader="true";document.head.append(script)})();